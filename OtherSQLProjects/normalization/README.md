# Database Normalization

This project demonstrates the step-by-step normalization of an unnormalized schema into 1NF, 2NF, and 3NF.

Each stage is implemented as a separate SQL file with explanations of:
- What changed
- Why the change was necessary
- Which anomalies were resolved