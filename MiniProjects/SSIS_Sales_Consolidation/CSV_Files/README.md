# CSV Files

This directory contains sample flat files (CSV format) used as **input sources**
for the SSIS Sales Data Consolidation ETL pipeline.

Each file represents sales data generated by a different store and is used to
simulate real-world batch data ingestion scenarios.