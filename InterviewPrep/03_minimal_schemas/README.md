# Minimal Schemas

This folder contains small, purpose-built schemas for SQL interview questions that do not fit the core schema.

Each file is self-contained and focuses on a specific concept such as:
- Event logs
- Hierarchies
- Enrollment relationships